# Fixtures

Hand-written test data shaped like real scraper output. Names are real GW1
entities but details (skill ids, stats, quest names, boss placement) are
approximations for testing — do NOT treat these as accurate game data.
The real datasets are produced by `/scraper` into `/data`.
